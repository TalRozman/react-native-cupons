import { Text, View } from "react-native"
import { styles } from "../style"

export const Home = () => {
    return (
      <View style={styles.screen}>
        <Text>HOME</Text>
      </View>
    )
  }