import { Text, View } from "react-native"
import { styles } from "../style"

export const Add = () => {
    return (
      <View style={styles.screen}>
        <Text>Add</Text>
      </View>
    )
  }